language: go
go:
  - 1.8.x
services:
  - postgresql
install:
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure
script:
  - go test -v $(go list ./... | grep -v /vendor/)
notifications:
  email: false
